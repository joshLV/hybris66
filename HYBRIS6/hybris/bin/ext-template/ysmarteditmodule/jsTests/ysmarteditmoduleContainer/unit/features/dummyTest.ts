import * as angular from 'angular';

describe('ysmarteditmoduleContainer - some test suite with TypeScript', () => {
	beforeEach(() => {
		angular.mock.module("ysmarteditmoduleContainer");
	});

	it('will assert that true equals true', () => {
		expect(true).toBe(true);
	});
});
